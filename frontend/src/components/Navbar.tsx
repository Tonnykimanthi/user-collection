const Navbar = () => {
  return (
    <header className="bg-primary-default py-4 text-white">
      <nav className="flex items-center justify-center">
        <ul className="text-lg font-medium">
          <li>Home</li>
        </ul>
      </nav>
    </header>
  );
};

export default Navbar;
